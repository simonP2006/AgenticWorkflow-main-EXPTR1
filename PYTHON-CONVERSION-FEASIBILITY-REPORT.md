# Python 코드 전환 가능성 정밀 조사 보고서

조사일: 2026-05-16
대상: WK 경비 정산 파이프라인 전체
조사 방법: 워크플로우 정의(`WK_workflow.md`)·PRD(`PRD_pre_instructions.md`) 1차 정독 + 7개 스크립트 정밀 코드 분석 + 실제 영수증 이미지 4종 육안 검증
핵심 가치: **정확성** — 추측 배제, 코드·이미지 근거 기반 결론

---

## 0. 결론 요약 (Executive Summary)

> **귀하의 가설은 정확합니다. 그리고 — 가장 중요한 사실 — 이미 ~90% 구현되어 있습니다.**

1. **"판독된 데이터를 Excel에 기입하는 것을 완전한 Python으로 처리"** — 이것은 가설이 아니라 **이미 완성된 현실**입니다. `write_excel.py`는 LLM에게서 평문 `(날짜, 시각, 금액)`만 받고, **어떤 셀에 넣을지는 LLM이 단 한 번도 결정하지 않습니다.** 요일 환산·셀 위치 탐지·금액 배치 규칙이 100% 결정론적 Python입니다 (근거 §2).

2. **"영수증 이미지 판독은 LLM 능력이 필요하다"** — 이것도 정확합니다. 그러나 *전적으로* 그렇지는 않으며, 그 이유가 정확성 관점에서 중요합니다 (근거 §3, §4).

3. 전체 파이프라인 9개 단계 중 **LLM 의존은 정확히 2개**(Step 3 OCR, Step 6 bbox 좌표)뿐이며, 나머지 7개는 이미 완전 결정론적 Python입니다.

4. **가장 중요한 발견 — 정확성의 진짜 병목은 "OCR을 Python으로 바꾸는 것"이 아닙니다.** 현재 시스템에는 **"LLM이 읽은 값이 영수증 이미지와 실제로 일치하는가"를 검증하는 결정론적 장치가 전혀 없습니다.** 오류가 "종종 생기는" 근본 원인은 여기 있습니다 (근거 §5).

---

## 1. 전체 파이프라인 — LLM vs Python 정밀 지도

`WK_workflow.md` 실행 절차와 7개 스크립트 코드를 1:1 대조한 결과:

| 단계 | 동작 | 실행 주체 | 결정론성 | 근거 |
|------|------|----------|---------|------|
| Step 1 | 이미지·앵커·매니페스트 추출 | **Python** (`extract_images.py`) | 완전 결정론 | ZIP/XML/openpyxl 파싱, LLM 입력 없음 |
| Step 2 | 카드 승인 데이터 추출 | **Python** (`extract_card_data.py`) | 완전 결정론 | 한글 헤더 매칭 파서 |
| Step 0 | 기존 OCR 삭제 | **Python** (`rm`) | 완전 결정론 | — |
| **Step 3** | **영수증 이미지 판독 → `ocr-results.json`** | **🔴 LLM (Claude Vision)** | **비결정론** | 날짜·금액·시각·인원·카테고리 |
| Step 4 | Excel 데이터 기입 (§4-§18) | **Python** (`write_excel.py`) | 로직 완전 결정론 | 셀 매핑 전부 Python (§2 참조) |
| Step 5 | Annotation 템플릿 생성 | **Python** (`generate_annotations.py`) | 완전 결정론 | 빈 bbox 템플릿 생성 |
| **Step 6** | **bbox 픽셀 좌표 식별 → `wk**.json`** | **🔴 LLM (Claude Vision)** | **비결정론** | 날짜/금액 영역 `[x1,y1,x2,y2]` |
| Step 7 | 수식 무결성 복원 | **Python** (`write_excel.check_and_restore_formulas`) | 완전 결정론 | ORG 대비 663개 수식 |
| Step 8 | RDR shape 주입 | **Python** (`annotate_receipts.py`) | 픽셀→셀 변환 완전 결정론 | per-image anchor calibration |
| Post | 카드 대조 검증 | **Python** (`verify_card_matching.py`) | 대조 알고리즘 완전 결정론 | consume 방식 전수 대조 |

**핵심**: 어떤 스크립트도 이미지에서 텍스트를 직접 OCR하지 않습니다. 이미지를 여는 3곳(`generate_annotations.py:107`, `annotate_receipts.py:307`, preview)은 모두 **픽셀 치수(`img.size`)만** 사용합니다. 텍스트 판독은 100% Step 3 LLM의 책임이고, 모든 스크립트는 LLM 산출 JSON을 **소비만** 합니다.

---

## 2. 귀하의 핵심 가설 — 이미 구현 완료 (입력 검증)

> "판독된 영수증 data들을 excel에 기입하는 것은 완전한 Python code로 처리"

`write_excel.py` 코드 분석 결과, **이 분리는 이미 엄격하게 관철되어 있습니다.**

| 작업 | 누가 하는가 | 근거 (file:line) |
|------|-----------|------------------|
| 날짜 → 요일 환산 | **Python** | `date_to_day` (`write_excel.py:31-34`) — LLM의 `weekday_mapping`을 역사전으로만 사용 |
| 요일 → Receipt 컬럼 | **Python** | `day_to_receipt_col` (`:37-39`) |
| 셀 위치 탐지 | **Python** | `detect_receipt_positions` (`:74-180`) — 라벨 스캔으로 동적 탐지 |
| 이미지 → 요일 매핑 | **Python** | `_match_images_to_days_by_order` (`:792-821`) — 좌→우 순서 1:1 분배 |
| 톨 1/2/3+ 분기 | **Python** | `:582-598` |
| 식사 시간대 분류 | **Python** | `hour<11 / <17` 규칙 (`:625-633`) |
| 통신비 80% 계산 | **Python** | `payment_amount*0.8` (`:744`) |
| 인원수 → FORM 동기화 | **Python** | §10-1/§11-1 동기화 로직 |

→ **LLM은 `(date, time, amount, headcount)` 평문만 제공하고, "이 금액을 어느 셀에"는 100% Python이 결정합니다.** 귀하가 의도한 할루시네이션 차단 구조가 이미 데이터 기입 전체에 적용되어 있습니다. 이 부분은 **추가 전환 불필요 — 이미 0% LLM**입니다.

---

## 3. 남은 LLM 단계 1 — Step 3 OCR: 전환 가능한가?

### 3.1 LLM이 실제로 하는 일 (`ocr-results.json` 스키마)

단순 글자 읽기가 아니라 **판독 + 의미 해석 + 분류**의 복합 작업입니다:

| 산출 항목 | 작업 성격 | 난이도 |
|----------|----------|-------|
| `toll_history[].amount/entry/exit` | 글자 판독 + 한글 지명 인식 | 높음 |
| `sunday_date` / `weekday_mapping` | 톨 첫 거래일 → 주(월~일) 일요일 추론 (PRD §2-§4) | 규칙화 가능 |
| `dinner[].meal_type` | 시각 → breakfast/lunch/dinner 분류 | **규칙화 가능 (이미 write_excel가 재계산)** |
| `staff_meetings[].headcount/names` | 영수증 하단 이름 카운트 + 소속 매칭 | 높음 |
| `telephone.month_matches` | 명세서 월 == 현재 월 비교 | 규칙화 가능 |
| 영수증 → 섹션 분류 | manifest 섹션 경계 기준 (이미 부분 규칙화) | 중간 |

> 의미 해석·분류 레이어 일부는 **이미 Python으로 흡수 가능**하며 실제로 `write_excel.py`가 `meal_type`을 시각으로 재계산합니다. 즉 LLM이 꼭 해야 하는 본질은 **"이미지 픽셀 → 날짜·금액·지명·이름 텍스트"** 추출입니다.

### 3.2 실제 영수증 이미지 육안 검증 — 결정적 증거

`research/images/`의 실제 영수증 4종을 직접 확인한 결과, **두 종류가 혼재**합니다:

| 유형 | 예시 | OCR 특성 |
|------|------|---------|
| **(A) 구겨진 감열지 사진** | `image4.png`, `image6.png`, `image10.png` | 휘어짐·기울어짐·저대비·접힘. 한글 가게명/지명. **전통 OCR(Tesseract/EasyOCR) 한국어 오인식률 매우 높음** |
| **(B) 깨끗한 디지털 영수증** | `image16.png` (카카오/앱 캡처) | 균일 폰트·고대비. 전통 OCR로도 판독 가능 |

육안 확인 사실: 감열지 사진들은 종이가 물리적으로 구겨지고 비스듬히 촬영되어, 숫자 `9700`이 `9100`으로, `960`이 `900`으로 **조용히 오인식(silent misread)** 될 전형적 케이스입니다. 가장 중요한 기준 영수증인 톨 "기간별 사용내역"도 감열지 계열입니다.

### 3.3 전환 가능성 판정: **부분만 가능 — 완전 전환은 정확성을 오히려 악화**

핵심 논리 — **귀하의 직관을 한 단계 더 정밀화해야 합니다:**

> "Python = 할루시네이션 없음"은 *데이터 기입*에는 100% 참이지만(§2), *이미지 판독*에는 그대로 적용되지 않습니다.

- 전통 OCR(Tesseract 등)은 "할루시네이션"은 안 하지만, 구겨진 감열지 한국어에서 **silent misread**를 냅니다. 이는 LLM의 오류보다 **더 위험**합니다 — LLM은 "11,900원인데 영수증엔 결제금액/과세표준/공급가가 여러 개 있다" 같은 **의미적 맥락**으로 올바른 숫자를 고르지만, Tesseract는 맥락 없이 가장 가까운 글자를 반환합니다.
- 따라서 Step 3 OCR을 **순수 전통 Python OCR로 전면 교체하면 오류가 줄어드는 게 아니라 늘어날 가능성이 높습니다.** 이 코드베이스가 픽셀 OCR 코드를 한 줄도 안 두고 LLM에 위임한 것은 **의도적이고 합리적인 설계 결정**입니다.

**가능한 전환 (정확성 향상 방향):**
- 유형 (B) 디지털 영수증: Python OCR 사전 추출 후 LLM이 교차 검증 → 하이브리드
- 의미 레이어(`meal_type`, `month_matches`, `sunday_date` 추론, 섹션 분류): **Python으로 완전 흡수 가능** — LLM 출력 스키마를 축소하여 LLM이 틀릴 표면적 자체를 줄임
- 톨 `direction`(go/back/stopover): entry/exit 지명 + 시각 순서로 **Python 규칙 도출 가능** (현재 LLM이 채우지만 PRD §16 규칙이 완전 결정론적)

---

## 4. 남은 LLM 단계 2 — Step 6 bbox 좌표: 전환 가능한가?

LLM이 영수증 이미지에서 날짜/금액 영역의 픽셀 `[x1,y1,x2,y2]`를 찍습니다. PRD §21은 **1mm 이내 정밀도**를 요구합니다.

- 픽셀→셀 변환은 **이미 완전 Python**입니다 (`annotate_receipts._per_image_pixel_to_cell:39-88`, per-image anchor calibration). 흡수 완료.
- 남은 LLM 작업(bbox *찾기*)은 **부분 전환 가능**:
  - OpenCV contour/edge detection 또는 template matching으로 "텍스트 블록 후보" 자동 제안 가능
  - 그러나 영수증 레이아웃 다양성 + 1mm 정밀도 + 구겨짐 때문에 **순수 Python 자동 검출은 신뢰 불가** → "Python 후보 제안 + LLM 확정" 하이브리드가 현실적 상한
- **즉시 가능한 Python 흡수 (정확성 직결)**: bbox **개수/위치 sanity-check**. MEMORY 기록상 WK09에서 LLM이 bbox를 83개(정상 ~29개) 과다 생성한 오류가 실제 발생했습니다. PRD/워크플로우에 "영수증당 정확히 2개, store_logo 제외" 규칙이 **프롬프트에만 있고 코드 가드가 없습니다.** 이 검증을 `annotate_receipts.py`에 결정론적 가드로 추가하면 해당 유형 오류를 **즉시 차단**할 수 있습니다.

---

## 5. 가장 중요한 발견 — 정확성의 진짜 병목

> **"종종 오류가 생기는" 근본 원인은 OCR이 Python이 아니어서가 아닙니다. LLM 판독 결과를 영수증 원본과 대조하는 결정론적 검증이 없기 때문입니다.**

현재 유일한 검증 `verify_card_matching.py`의 사각지대 (코드 근거 `verify_card_matching.py:287-370`):

- 이 스크립트는 **"Receipt 시트 금액 ↔ 카드 승인 내역"**을 대조합니다.
- Receipt 시트 금액은 LLM OCR에서 파생됩니다.
- 즉 **"LLM OCR ↔ 카드내역"** 일치만 검증할 뿐, **"LLM OCR ↔ 영수증 이미지"** 일치는 어떤 스크립트도 검증하지 못합니다.
- TELEPHONE/TOLLS는 카드 대조에서 아예 제외(별도 카드)되므로 — **톨 금액(가장 기준이 되는 영수증)은 결정론적 검증이 0건**입니다.

**결론**: LLM이 톨 960원을 900원으로 잘못 읽어도, 현재 파이프라인은 이를 잡아내는 결정론적 장치가 없습니다. 이것이 "정확한 금액을 정확한 위치에" 실패하는 실제 메커니즘입니다.

---

## 6. 권고 — 정확성 극대화 우선순위 (절대 기준 1)

전환 대상을 "LLM→Python"이 아니라 **"검증되지 않는 LLM 출력 → 결정론적으로 검증되는 LLM 출력"**으로 재정의하는 것이 정확성에 가장 효과적입니다.

| 우선순위 | 조치 | 성격 | 정확성 효과 | 난이도 |
|---------|------|------|-----------|-------|
| **P0** | bbox sanity-check 가드 (영수증당 N개, 로고 제외, 총량 상한) `annotate_receipts.py`에 추가 | Python 흡수 | WK09형 과다 bbox 오류 **즉시 차단** | 낮음 |
| **P0** | LLM OCR 출력 스키마 축소 — `meal_type`/`direction`/`sunday_date`/`week_number`/`month_matches`/섹션분류를 Python 재계산으로 일원화 (LLM은 raw text만) | LLM 표면적 축소 | LLM이 틀릴 항목 수 자체 감소 | 중간 |
| **P1** | **이중 판독 대조(Self-Consistency)**: 동일 영수증을 LLM이 2회 독립 판독 → Python이 불일치 항목만 플래그 → 사람/재판독 | LLM+Python 검증 | silent misread를 결정론적으로 표면화 | 중간 |
| **P1** | 톨 영수증 산술 무결성 검증: `toll_history` 일자별 금액·방향이 PRD §16 거리표/패턴과 모순되면 FAIL (예: go인데 금액 0, 동일 구간 금액 불일치) | Python 결정론 검증 | 톨(검증 0건 사각지대) 봉쇄 | 중간 |
| **P2** | 디지털 영수증(유형 B)에 한해 Python OCR 사전 추출 → LLM 교차검증 하이브리드 | 부분 전환 | 깨끗한 영수증의 독립 검증 채널 확보 | 높음 |
| **참고** | 구겨진 감열지(유형 A) 순수 Python OCR 전면 교체 | — | **권장하지 않음** — silent misread로 정확성 악화 | — |

---

## 7. 최종 답변

| 귀하의 명제 | 판정 | 근거 |
|------------|------|------|
| "이미지 판독은 LLM 능력이 필요하다" | **참** (단, 의미 레이어는 Python 흡수 가능) | 구겨진 감열지 한국어 영수증 — 전통 OCR silent misread (실제 이미지 검증, §3.2) |
| "데이터 기입은 완전 Python으로 처리하는 게 할루시네이션을 줄인다" | **참 + 이미 100% 구현됨** | `write_excel.py` — LLM은 셀을 단 한 번도 결정 안 함 (§2) |
| "프로젝트 전체를 얼마나 Python으로 전환 가능한가" | **9단계 중 7개 이미 Python. 남은 2개 중 1개(bbox)는 부분 전환, 1개(OCR 픽셀 판독)는 본질적 LLM 영역** | §1, §3.3, §4 |

> **핵심 교정**: 더 전환할 Python 코드를 찾는 것보다, **이미 LLM이 하는 2개 작업의 출력을 결정론적 Python으로 검증·축소하는 것**(§6 P0·P1)이 정확성 향상의 결정적 지렛대입니다. 현재 시스템의 정확성 결함은 LLM 사용 자체가 아니라, **LLM 출력의 미검증 영역(특히 톨 금액)**에 있습니다.
