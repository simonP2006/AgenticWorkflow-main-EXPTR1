# WORKER_TODO — EXPENSERECEIPT build worker (ws1/surface:2)

> **Identity (DE-MASTER)**: I am the EXPTR1 expensereceipt-skill **build worker**, NOT master/CSO. Config = EXPTR1 CLAUDE.md + AGENTS.md + soul.md. Ignore spJavis master persona / WK23 / store-db restore context.
> **Reporting**: plain-text push to master `surface:1` (`cmux send --surface surface:1 "..."` + `cmux send-key --surface surface:1 Return`). NO modal/AskUserQuestion.
> **Constraints**: zero skill output until master approves M0. No git / no external send / no irreversible delete. denylist §4 → stop + push.
> **Why namespaced filename**: existing `WORKER_TODO.md` belongs to the WK23/store-db thread → must not overwrite (§4). This is my file.
> **Key docs**: SPEC `EXPENSERECEIPT-SKILL-DESIGN-SPEC.md` (LOCKED SOT) · plan `EXPR-MILESTONE0-BUILD-PLAN.md` · survey digest `/tmp/expr_survey_digest.md`.

## M11 — AR distortion fix (2026-06-25, master /goal, autonomous)
- [x] **ROOT CAUSE**: `-place` placed receipts as `twoCellAnchor` (from→to box) → Excel STRETCHES image to fill box, ignoring native AR + orientation → all 7 WK23 receipts distorted (squish 37–167%), portrait KICC/Tworld "lying down". Systemic (WK21 also 12/12 stretched). NOT EXIF/rotation — pure box-stretch. Verify never checked displayed-AR/orientation (same blind-spot class as the amount-fill miss).
- [x] **FIX (deterministic, 결정론 환원)**: `scripts/fix_receipt_anchors.py` — converts each `<pic>` twoCellAnchor → `oneCellAnchor` + `<xdr:ext cx cy>` sized to NATIVE pixel AR (uniform scale, cap≤1.0 never-upscale, letterbox, center). AR exact by construction. **Pixels untouched** (only drawing2.xml; 13 media md5-identical). Applied to WK23 deliverable (backup `.prefix_bak`).
- [x] **PERMANENT**: folded into `expensereceipt_place.py` `place()` (calls re-anchor) + **fail-closed AR gate** (the missing gate) — non-vacuous (independent `verify()` counts surviving stretched/EXIF-rotated pics).
- [x] **§5 adversarial panel** (4 lenses, evidence-backed) found 5 defects → ALL fixed + locked as regression tests: CRITICAL EXIF-transpose in `_to_png_bytes` + EXIF-aware verify; MEDIUM vacuous-gate → independent verify; LOW degenerate-dims fail-loud; LOW renderer (visual proof rendered); LOW extreme-AR (no real impact).
- [x] **VERIFIED**: WK23 7/7 squish 0.000% / orientation preserved / 0 EXIF-rotated; only drawing2.xml changed; amounts+M52+formulas intact; no overlap; openpyxl-clean. Cross-week WK21 12/12. **Suite 328 PASS / 0 FAIL.** Visual before/after proof delivered. ⟶ 입력 read-only, 비가역 없음.

## Status (2026-06-20)
- [x] M0.1 Read directive `/tmp/expensereceipt_worker_directive_0620.txt` + identity correction.
- [x] M0.2 Read SPEC + EXPTR1 CLAUDE.md/AGENTS.md/soul.md.
- [x] M0.3 Asset survey (10 read-only agents) → digest internalized. Located all assets in EXPTR1 tree.
- [x] M0.4 Synthesize build plan → `EXPR-MILESTONE0-BUILD-PLAN.md` written.
- [x] M0.5 Write this WORKER_TODO.
- [x] M0.6 Pushed ACK to master surface:1 (identity + plan gist; both send + send-key Return = OK).
- [x] M0.7 **M0 APPROVED** by master (independent Workflow 7-agent CONFIRMED14/REFUTED0 + gemini §5). 11 guardrails + decisions reflected as binding into plan §3/§8/§11 + this file.
- [x] **M1 DONE** — 7 skill dirs + `expensereceipt/references/authoring-spec.md` + 7 SKILL.md shells. Self-verify PASS (`/tmp/verify_m1_scaffold.py`): desc≤1024 (963/862/774/813/790/821/760), frontmatter↔folder, master model-invocable + 6 subs disable-model-invocation:true, DNA 9-genome + 3 Constitutional all present. Pushed to master → awaiting M1 approval.
- [x] **M2 `expensereceipt-db` DONE** — `scripts/expensereceipt_db.py` (ledger / rollup PORT build() / gallery G10 metadata-key / classify-db / quarantine→snapshot→promote→rollback PORT). 실측 PASS 21/21 (`--selftest`): ledger round-trip+idempotent, rollup section_dist/confidence/typical, gallery store-tailored few-shot + cold-start empty, **txn rollback→store-db IDENTICAL (master 실측 요건)**. Genuine reuse norm_store/parse_date (_REUSED=True). Namespaced planning/expensereceipt/ (WK store-db.json untouched). Caught+fixed PROJECT_DIR parents[3]→robust finder bug. Pushed → awaiting M2 approval.
- [x] **M3 `expensereceipt-merchant` DONE** — `scripts/expensereceipt_merchant.py`. validate_biz_no() = STANDARD NTS weights [1,3,7,1,3,7,1,3,5]+tens(d9×5) (★brief's literal [..5,1,3] was a typo: 1/15; standard = 15/15 real numbers — empirically derived from store-db.json). 실측 PASS 13/13: 15 real VALID + 15 corrupted INVALID + hyphen-strip + G3 best-effort probe(사업자번호↔사업자등록번호) + NO_CARD fallback + merchant_key==receipt_key consistency. _REUSED=True (IMPORT _biz_no/match_card/norm_store/_merchant_name/_category from build_store_db, receipt_key from classify_section). Exposes validate_biz_no for M7. **Also fixed M2 cross-consistency**: -db _mkey was stripping biz_no→digits; corrected to hyphenated (receipt_key-consistent); M2 re-verified 21/21. Pushed → awaiting M3 approval.
- [x] **M4 `expensereceipt-extract` DONE (deterministic parts)** — `scripts/autodetect.py` (folder/file-kind NFC + G11 minimal preprocess; 실측 13/13, real WK23 6 files classified correctly) + `scripts/expensereceipt_vote.py` (verbatim-core PORT aggregate_ocr_votes: _vote/existence-then-field/STRONG=ceil0.8k/MIN3/MAX7/exit0-2-1, flat receipts schema, ident=(date,amount) time-excluded, vote amount/people/handwriting; 실측 14/14). Vision OCR+필기 = LLM HALT spec in SKILL.md (2 HALTs, independence, G10 gallery few-shot, ANCHOR#2b escalation). Test fix: T8 amount-in-identity→phantom (not silent correct) — corrected to people field-vote demo. Pushed → awaiting M4 approval.
- [x] **M5 `expensereceipt-classify` DONE** — `scripts/expensereceipt_classify.py`. 6-sector SPEC §2 tree: det triggers(toll/tel), handwriting→A999 company(TERADYNE→STAFF/SAMSUNG→TRAVEL, Samsung-present→TRAVEL incl ≥3 mixed), unknown name→escalate, ≥2→escalate, G9 same-date STAFF(a), solo-lunch→OTHERS, store-db T1 Dinner→Dinner, ambiguous dinner→escalate(LLM/owner). G2 dynamic read_name_database(reads past 1007). G5 section-predictions.json write + section-confirmed.json read-only + exit2 HALT. IMPORT classify_section.classify/build_store_db/read_name_database (_REUSED=True). 실측 21/21. **Also M2 3rd fix**: _SECTOR_TO_LEARNING mapping (SPEC sector→short store-learning label) so classify_section works; M2 re-verified 21/21. Pushed → ★master verify + first gemini §5 gate.
- [x] **M6 `expensereceipt-place` DONE — SAFE-verified + G7 doc-honesty finished** — `scripts/expensereceipt_place.py`. ★Surgical `<pic>`+rels+media injection PROVEN vs REAL template: drawing2.xml(Receipt=sheet3), creates drawing2.xml.rels(absent), media image7+, verbatim zip rewrite+os.replace. layout_3_per_row(reverse _get_receipt_image_anchors). zip_drawing_counts PORT. G8 candidate→physical_verify→atomic adopt/rollback (입력 immutable·출력 raw-data/output). **G7 insert_rows_lxml = FAIL-LOUD**(raises InsertRowsNotImplemented; 부분시프트 침묵 desync 금지)·plan_insert_rows ESCALATE 센티넬·physical_verify에 row_cell_consistency backstop. G13 NFC. Q4: OTHERS-LOCAL=[G] confirmed(실측); per-sector geometry→M9. **master 3중검증 SAFE(high/crit 0)**. ★G7 DOC-HONESTY 마감(승인조건): SKILL.md frontmatter/docstring/counts·DESIGN-SPEC(.bak+changelog)·authoring-spec·build-plan·WORKER_TODO 전 잔재 정정 + POSITIVE desync selftest(탐지기 실패방향 발화 증명). 실측 **22/22**(exit0). Pushed → master 재검증·M6 최종승인·M7 인가.
- [x] **M7 `expensereceipt-verify` DONE** — `scripts/expensereceipt_verify.py` (★ANCHOR#2a mandatory gate). 7 checks: V1 biz-checksum(IMPORT validate_biz_no)·V2 card cross-match(consume reconcile+★G1 canceled-defensive[cancel-named col OR explicit cancel-status value·key-absent⇒not-canceled]+★G3 NFC)·V3 item-sum==card(NEW conditional)·V4 amount≠0·V5 handwriting-confidence(NEW)·V6 consensus=★G4 ocr-vote-audit.json not compare()·V7 rule-consistency(IMPORT classify_receipt). ★G6 2-split run_logical(pre-place)/run_physical(post-place, IMPORT place.zip_drawing_counts/physical_verify). Result/verdict PORT verify_week(raise→SKIP, exit0 iff no violation-FAIL). read-only·JSON-only(no FAIL.xlsx). _REUSED 전부 True(in-process producer IMPORT, Caveat). G6 PHYSICAL 실제 placed xlsx 검증. **★FAIL-CLOSED 하드닝**(self-audit ⒜⒝ + master wpcln51sn 9건): ERROR(unrun mandatory: producer부재/check raise/required input부재)를 deliberate N/A-SKIP(V3 항목無·V6 단일read)와 분리 → ERROR⇒verdict≠PASS·exit≠0. V1 producer부재→ERROR(weak fallback 제거)·V1b biz presence warning·V3 None item→ERROR·V6 multiread-expected absent→ERROR·V7 classify/store_db부재→ERROR·run_physical -place부재→ERROR·_run raise→ERROR·_verdict fail-closed·report에 _REUSED+per-check kind surface. 실측 49/49(exit0): 7체크 PASS+FAIL + 9 fail-closed 주입(a)-(h) + N/A 회귀(clean유지). python 검증(ugrep금지). Pushed → master 재검증+경량 re-audit(ANCHOR#2a).
- [x] **M7 최종승인 ✅** (§5 re-audit w7yw2j6c2 CLEAN: 8 degradation fail-closed·injection non-vacuous·_verdict ANY-ERROR block·회귀0 + master 49/49).
- [ ] **M8 `expensereceipt` master orchestrator** — ★인가 보류(주인님 stock-db vs EXPR 우선순위 결정중). 설계 사전구상 OK·빌드 착수는 인가 후. 설계요지: PORT run_week.py 재진입 harness(stage table + done()/vision_check() lambda + exit-code routing 0/10/1/20 + emit()/_flush() orchestration trace). 6 sub 조율(db→merchant→extract→classify→verify-logical→place→verify-physical→db). ★store_db 항상 run_logical 전달(HIGH#6). ★G6 2-split: -place는 verify-logical PASS 후에만 mutate, 직후 verify-physical. ★G5: classify HALT 핸드오프 표면화→master가 section-confirmed.json write→재실행. ★G12: classify escalation 중 ≥0.95 dinner-venue 고확신건만 근거기록 auto-confirm·진짜모호는 owner escalation. orchestration trace 4섹션 synthesis(SPEC §1). ← STANDBY (M8 directive 대기)
- [x] **M8 `expensereceipt` master orchestrator DONE** — `scripts/expensereceipt_orchestrator.py`. ★M7 fail-closed 계약 강제: gate_and_place=LOGICAL verify→(PASS시만)place→PHYSICAL re-verify, ERROR/violation-FAIL⇒place 절대안함·HALT·escalate(N/A-SKIP만 통과). ①store_db 항상전달(real verify로 store_db=None→V7 ERROR→HALT·store_db={}→PASS→place 입증) ②③G6 2-split. G12 decide_escalations(≥0.95 dinner auto-confirm+근거·모호→owner). G5 write_section_confirmed(master-write). G13 NFC. orchestration trace. dependency-injection으로 ★NON-VACUOUS 검증(place-spy: not-clean시 place 0회 호출 증명). 실측 22/22(exit0). master model-invocable 확인(frontmatter name+desc only, disable-model-invocation 무). 8 scripts 전 suite 빌드완료. Pushed → master verify + §5.
  - **M8 §6-b owner-control fix FINAL 승인** ✅ (26/26: unresolved owner-escalation→실제 HALT·place HELD·non-vacuous spyO=0; G12-auto만 section-confirmed; gate_and_place 무변경; 회귀PASS). **★M0-M8 전체 8-스킬 스위트 빌드 완료·전부 FINAL 승인** (3 §5 gate + keystone audit 통과).
- [x] **M9 통합 실정산 — ✅ 완료(금액+이미지 전부·전 검증 PASS·M52=115,560)** — 입력 raw-data read-only·출력 raw-data/output/만. (금액누락 CRITICAL 발견→근본원인 진단→write_excel 금액기입+openpyxl add_image 이미지로 정정 완료.)
  - [x] **M9.10 ★FILL 완료: 금액+이미지+신규게이트 PASS (2026-06-22)**: master (X) openpyxl add_image 승인. 7 영수증→Receipt drawing2(승인crops 3+폴바셋/toll/KICC/Tworld·place geometry anchor·PNG 무손실). pic 6→13·drawing 1/2/3 구조 복원·resolve_receipt_drawing=Receipt 정확. ★신규게이트 PASS: cell-occupancy 9/9·pure-python 총액==115,560·SUM공식 보존·M52 공식 intact·pic Receipt배치확인·embedded dims==source·verify-PHYSICAL P1-P5 exit0. 입력 7/7 byte-unchanged·WK00 원본 무접촉·store-db dd4e8453 불변. 산출물=raw-data/output/simon_park_T&E_WK23_2026.xlsx(3.65MB). D 영구통합 권고=SUITE_AMOUNT_FILL_RECOMMENDATION.md(주인님 기상후). surface:1 완료보고.
  - [x] **M9.11 ★WK23 정산 최종완료·삼중검증 수렴·STANDBY (2026-06-22)**: 삼중 독립검증 전부 수렴 — 워커 자체게이트+verify-PHYSICAL · master §5 `w4jl7xrrg` 3/3 SOUND · CSO 5/5 CLEAN. 최종 산출물 115,560원·9 금액셀+7 이미지·twoCellAnchor 보존·입력 7/7 불변·템플릿 무손상. master 확인: WK23 정산 최종완료. **추가작업 전면보류** — suite 영구픽스/git/teardown=주인님 기상후 결정. 로컬서버 0(종료 불요). **STANDBY — 입력 read-only 유지, 다음 지시 대기.**
  - [ ] **M9.7 ★CRITICAL 금액-셀 누락 (master 독립검증 2026-06-22)**: 산출물 simon_park_T&E_WK23.xlsx 무손상·이미지 13개 정확하나 7 영수증 금액(22800/8300/8700/9500/3120/3300/74800)이 워크북 어디에도 없음·FORM!M52=0. 근본: place=이미지만(<pic> 주입)·금액 transcription(write_excel.py 보유 로직) 미이식/미호출·orchestrator에 fill 단계 부재. 내 4계층 검증이 못잡음(verify-PHYSICAL=image count/anchor만·금액-셀 체크 부재). ★핵심제약: WK23 산출물 이미지 존재→openpyxl 금액기입시 anchor 파괴→fix=template 금액선기입→place REDO 순서. 진단 Workflow wkmskc4qh 완료(verdict=PLAN_READY). 책임=SUITE_GAP_UNBUILT(스위트에 금액-셀 기입 단계가 처음부터 없음·write_excel.py amount-writer 미호출). ★Critical 2건 발견: (A)전화비 80% — A601=74800×0.8=59,840(내 보고 130,520은 틀림·올바른 M52=115,560) (B)FORM!K8 stale=2026-03-15(→2026-06-07). 정확 9-셀맵 확정(B401/C401/E401/E124/A601/B6/E6/B10/D10·F6/F10 SUM공식 보존). Fix=clean 템플릿부터 REDO(openpyxl 금액기입→공식복원→place surgical-zip; 현 산출물 openpyxl 패치=이미지 앵커 파괴). 신규 cell-occupancy+M52 정합 게이트 필요. 근거+계획 전문=ROOT_CAUSE_AND_FIX_WK23_AMOUNTS.md. surface:1 보고+의사결정 4건(A전화80%·B K8주체·C fix경로·D 스위트영구수정) 요청. **배포금지·정산 미완·수정 미실행 — master 결정 A/B/C/D + fix 승인 대기.**
  - [x] **M9.8 master §5 fix계획 승인(조건부) + month_matches 판정 (2026-06-22)**: master Workflow wnqnwk16p — 셀맵/method(i)/순서/K8자동 SOUND 확인. 게이트 정정1(Σ셀==M52 cached는 openpyxl 미재계산→false-FAIL; pure-python 기대총액 assert로 대체·M52는 구조보존만)·정정2(image-free→Receipt-pic-free). D=영구통합. ★pre-fill month_matches 판정 보고: **TRUE** — WK23 청구서 라벨 "6월 이용요금" 74,800(vision 재확인)=WK23 정산월 6월 MATCH(PRD §12-3); 선례 WK10 동일(3월주 74800 true→59840); write_excel L879/1469 payment×0.8 if month_matches. ⟹ A601=59,840·M52=115,560(55,720 아님). SOT 130,520→115,560 정정. surface:1 보고. **★master month_matches 확인 후 fill 시퀀스 착수 — 그 전까지 수정 미실행·입력 read-only.**
  - [x] **M9.9 FILL: 금액기입 완료 + ★이미지 합성 BLOCKER + 해결안 검증 (2026-06-22)**: master FILL GO(month_matches=TRUE 승인) 수신. broken 산출물 백업·clean WK00(f33078a5) output슬롯 stage·research/ocr-results.json+card-approval-data.json owner-faithful encode(toll Go<Back 시간순)·week 자동도출=23. ★write_excel.py --all 성공: 63셀 변경(9 영수증금액+headcount+FORM KRW+attendee roster+Mileage+G2/K8) 전부 정확. cell-occupancy 9/9 OK·SUM공식 보존·M52 공식 intact·pure-python 총액=**115,560** 일치. ★★BLOCKER 발견·검증: "openpyxl 금액→스위트 surgical-place" 합성불가(openpyxl이 Receipt placeholder drawing 드롭→drawing2=Instructions됨→place가 영수증을 Instructions에 오배치·place_images는 Receipt drawing 신규생성 못함). ★해결안 실측검증: openpyxl add_image가 Receipt drawing2 신규생성+Instructions drawing3 복원+chrome 보존(resolve_receipt_drawing=Receipt 정확). surface:1 보고+방식 확인요청(X openpyxl add_image[권고] / Y legacy annotate / Z surgical amount-fill 신규). **★master 이미지방식 결정 대기 — 그 전까지 이미지배치 미실행·입력 read-only·amounts 산출물 준비됨.**
  - [x] **M9.6 PHASE1 place→verify-PHYSICAL→db→byte-diff 완료 (2026-06-22)**: 주인님 place GO 수신 → place 7 영수증(TRAVEL 3/줄 폴바셋·까치201·까치386 @row407 · Dinner 스팟마트 @row125 · PARKING/TOLLS 풀폭 하이패스 @row4+KICC @row83 · TELEPHONE Tworld @row601; WK23.pdf 3장=승인 crops) → 산출 raw-data/output/simon_park_T&E_WK23_2026.xlsx (3,643,236 bytes·pic 6→13). G8 valid + 독립 verify-PHYSICAL P1-P5 PASS. db: ledger_add 4 learnable → quarantine → promote(verdict=PASS) → suite-own DB planning/expensereceipt/(스팟마트 DINNER·까치화방 TRAVEL×2·폴바셋 TRAVEL·snapshot). ★run후 입력 byte-diff 7/7 불변(card bd8e1d0c 포함)·WK00 원본 무접촉·WK store-db dd4e8453 불변. 섹터합 130,520원. surface:1 완료보고. **master 독립검증 대기.**
  - [x] **M9.0 dry-run 계획(§6-2) 작성·push 완료** (2026-06-21, WK23 실행워커): WK23_SETTLEMENT_DIRECTIVE.md + WORKER_TODO + DESIGN-SPEC + HARDENING_CAMPAIGN 정독 → `DRYRUN_PLAN_WK23.md` 작성 → surface:1 평문 push(send+enter). 입력 8 entries(7실+.DS_Store) md5 PRISTINE 확인(pdfinfo 전후 md5 동일). autodetect 처리경로 매핑·예상섹터·6 HALT지점·WK22 stale 리셋방법(백업이동·store-db 보존) 정리. ★핵심발견 push됨: receipt-kind 소스 3개 vs owner 필기영수증 4건 = multi-receipt 존재→H1 첫 HALT 예상. 정정 반영: 6/1 폴바셋 = 김병수·우석원·이정민·me(드→me). **STOP — master 승인 대기, place/실행 안함.**
  - [x] **M9.1 §5 보강(v2) + owner 인벤토리 확정 반영 + ★입력 무결성 HOLD** (2026-06-21): master §5 APPROVE_WITH_CONDITIONS 7조건 반영 → DRYRUN_PLAN v2. adversarial Workflow `w6pbu0vz8`(4-agent READ-ONLY, PASS): stale move-list 16파일 검증(★research/ocr-results*.json 4개 = wk22 archive와 byte-identical md5 aca53b04=garbage-in 실증), preserve 21+2dir, ★NEW dir-level 벡터 발견(research/images HIGH 등 — master top-level 규칙 미커버), 재검증기준 정련(confirmed:true→0·WK22는 store-db* 제외), store-db 스팟마트 119-81-00851 DINNER conf1.0 cross-check. owner 인벤토리 확정(§3-4) 반영: 까치201=★이창성(이정우 vision오독 정정)·까치386=이종희·스팟마트=혼밥·폴바셋4명·하이패스 toll 포함·KICC주차·Tworld telephone. ★★입력 무결성 경보: 카드 xls 8ecb0ae9→bd8e1d0c(size동일·mtime보존·ctime점프=OLE 메타 재기록 시그니처, blast radius=이 1파일만, 나머지 6 PRISTINE). **전면 HOLD — stage/place/실행/orchestrate 일체 금지, 입력 무접촉 read-only, master 추가 GO 대기.**
  - [x] **M9.2 무결성 ✅해소 + dir-level 승인 반영 (2026-06-21)**: master 통지 — CSO 포렌식 BENIGN(8ecb0ae9 원본 vs bd8e1d0c xlrd 셀대조: card 7행 byte-identical, md5차=OLE 메타만)→data-intact precondition 충족·재export 불요. dir-level 백업이동(images/HIGH·ocr-enhanced·ocr-crops/grid 4개) master 승인→reset move-list 포함. DRYRUN_PLAN v2 갱신: §0-d RESOLVED-BENIGN·§6-B 승인반영·§7 -1 precondition 충족·§9 단일게이트(주인님 최종GO→phase1 신호). **여전히 실행 전면보류 — phase1 신호 대기, 입력 read-only 유지.**
  - [x] **M9.3 PHASE1 §7-0 stale-reset 집행 + §6-D 재검증 CLEAN (2026-06-21)**: master PHASE1 GO 수신 → 16파일+4dir MOVE→`_stale_backup_wk22_20260621_232458/`(가역·RESTORE_NOTE 동봉·MISSING 0). §6-D 재검증 CLEAN: WK22_2026 grep(store-db* 제외)=0·confirmed:true=0·store-db 일가 INTACT(store-db.json md5 dd4e8453 불변·quarantine/snapshots/names-data 보존)·입력 무접촉(card bd8e1d0c·WK23.pdf 85d9594b·template f33078a5 불변). surface:1 보고. **stage_inputs 前 HOLD — master 독립검증 후 계속신호 대기.**
  - [x] **M9.4 PHASE1 파이프라인 stage_inputs→verify-LOGICAL PASS (2026-06-21)**: master CP1 CLEAN 계속신호 수신 → stage_inputs(7파일 staged md5=source 일치·원본 무접촉) → autodetect(staged: card_stmt·template·KICC slip·telephone·receipts 3) → 공식 vision OCR 7문서(staged 사본; 전부 owner값 일치·명백불일치 0·HALT 없음) → V2 카드대조(카드승인내역 5행 1:1 consume) → merchant NTS 4 biz VALID → classify(name-DB Receipt!A999:H1007: 이창성/이종희→SAMSUNG EDS·김병수→HBM-PE·우석원/이정민/me→TERADYNE) → escalation 0 → **verify-LOGICAL PASS exit0**(V1-V7, V3·V6 N/A). 6섹터 매핑(합 130,520: PARKING/TOLLS 6,420·TEL 74,800·Dinner 9,500·TRAVEL 39,800; STAFF/OTHERS 0) research/expensereceipt/wk23-run/sector-mapping.md. 입력 7파일 전부 불변 확인. **★place 前 STOP — 주인님 확정 2건 대기: WK23.pdf 3-in-1 분할방식 + 6섹터 매핑표 최종승인.**
  - [x] **M9.5 6섹터 매핑 주인님 승인 + WK23.pdf 자동크롭 (2026-06-22)**: 주인님 매핑 '맞다' 승인 + 분할=(나)워커 자동크롭+review 결정. wk23-1.png(1446x2000) 행-darkness 갭분석→클린 컷 y725/y1285로 3장 분할 → expr-place-preview-WK23/crops/ 저장(crop1 스팟마트 DINNER·crop2 까치201 이창성 TRAVEL·crop3 까치386 이종희 TRAVEL + CROP_MANIFEST.md). ★3 크롭 vision 재판독 검증=각 1장 완전·잘림/겹침 0. 입력 무접촉·staged 사본만. surface:1 보고. **★STOP — place 안함, master 크롭검수+주인님 확인 후 place 신호 대기.**
- [x] **M10 rename-8단계 — 검증완료(이름 이미 최종형)**: frontmatter↔폴더명 7/7 일치·dmi(master無/6subs true)·cross-links 일관·stale build-name 0·settings.json rename무관·project-scope install 확인(SPEC L50)·discovery 작동(harness가 expensereceipt model-invocable 발견)·LOW row_gap 16 근거주석(R204 재현·18은 R206 어긋남)·백업 /tmp/_expr_backup_m10_pre·최종 suite 198/0(staged)·입력 8ecb0ae9 불변. ★남은 결정: user-scope 심볼릭(전역 discovery vs cross-project 오염)=master/owner 결정 대기(project-scope 작동중→선택사항).
- [ ] M3 `expensereceipt-merchant` (NEW NTS checksum + merchant key).
- [ ] M4 `expensereceipt-extract` (autodetect + OCR + 3→7 vote + handwriting).
- [ ] M5 `expensereceipt-classify` (6-sector engine + name-DB) — §5 gemini.
- [ ] M6 `expensereceipt-place` (surgical direct-zip + 3-per-row engine) — §5 gemini.
- [ ] M7 `expensereceipt-verify` (7-check gate, IMPORT producer rules) — §5 gemini, ANCHOR #2a.
- [ ] M8 `expensereceipt` master (orchestrator + trace + 4-section synthesis).
- [ ] M9 Integration dry-run on real WKnn (HALT loop, all gates PASS).
- [ ] M10 rename-8단계 + install/symlink + discovery re-validation.

## Key reuse decisions (from survey — evidence-backed)
- Card statement = **spreadsheet** `카드승인내역*.xls(x)`, NOT PDF → IMPORT `extract_card_data` parsers.
- **NTS checksum = NEW** (grep-confirmed absent) — only major net-new logic. Algo: weights [1,3,7,1,3,7,5,1,3], 9th×5 tens-digit, sum, (10−sum%10)%10==10th.
- Surgical-zip = **`annotate_receipts.py`** (not write_excel.py); injects `<sp>` → image `<pic>`+rels+media = NEW; 3-per-row coordinate engine = NEW.
- STAFF↔TRAVEL = **A999 company-column** (TERADYNE=STAFF / SAMSUNG EDS·HBM-PE=TRAVEL).
- Voting NOT a correctness oracle → `-verify` is mandatory downstream defense.
- Authoring: foresight pattern; desc **≤1024 chars** (detail→body); sub-skills `disable-model-invocation:true`.
- name-DB range is **dynamic** (not hardcoded H1007).

## Decisions + binding guardrails (M0 approval — see plan §11)
**Decisions**: Q1 input=`raw-data/input/WKnn_2026/` (owner-confirmed, `~/WKww_YYYY` overridden). Q2 canceled rows excluded from -verify (via G1). Q4 OTHERS-LOCAL @ M6. Q5 read-only by code (JSON-only). Q3 (annotate_receipts.py) within-roadmap.
**11 binding guardrails** (applied at named milestone): G1 canceled defensive-detect[M7] · G2 dynamic name-DB only, no 1007-ceiling[M5/6] · G3 card raw keys best-effort+NFC[M3/4/7] · G4 consensus=ocr-vote-audit.json not compare()[M7] · G5 section-confirmed.json=master writes, not sub-skill[M5/8] · G6 verify 2-split pre/post-place[M7/8] · G7 insert-row=FAIL-LOUD(raises; formula-aware shift NOT implemented), openpyxl-insert FORBIDDEN, pre-sized bands primary, true overflow→ESCALATE[M6] · G8 candidate-output+os.replace rollback[M6] · G9 STAFF "Dinner registered"=same-date[M5] · G10 gallery few-shot=metadata key not pixels[M2/4] · G11 preprocess minimal=rotate+grayscale[M4] · Caveat verify leaf=in-process IMPORT, strip layout deps[M7].
**Flag (non-blocking, M5)**: solo lunch → OTHERS-LOCAL per SPEC; TRAVEL-rescue=owner decides.

## Build discipline (every milestone)
build → 실측 self-verify (`python3 -c` / actual run, "확인했다" not "될 것") → push master → master verify + §5 dialectic + approval → next. Autopilot serial, 인내심, no skipping, quality absolute.
