# EXPENSERECEIPT HARDENING CAMPAIGN — master to-do / journal

> Owner-directed (2026-06-21). Methodology = "cys-insight-report" rigor applied to the **expensereceipt** skill.
> Scope decision = **Option A** (owner-confirmed): harden the skill's deterministic, in-scope behavior to 100%; hand image-editing / free-text / FORM-header are accepted as the owner's manual craft (OUT of scope).
> Budget/stop: all-items-PASS via 10 fresh test prompts (100% accuracy, different prompts each round, zero hallucination, source-backed) — OR stop after 25 turns.
> Original skill = UNTOUCHED. Work on a COPY; merge to production only after full PASS + owner approval.

## Scope (locked)
- **IN (harden to 100%, deterministic, zero-hallucination):** 6-sector classification + escalation; OCR extraction + multi-read voting; card cross-match (consume-once) + biz-no NTS checksum; sector-band placement correctness; untouched-paste image placement; db/ledger/store-db SOT integrity; verify fail-closed 7-check gate; **deterministic-reduction** (move LLM-rereasoned fact-lookup / number-mapping / date-calc / range-check / existence-check into Python the SKILL.md must call); SKILL.md↔code honesty.
- **OUT (owner manual craft, accepted):** hand image-editing (segment multi-receipt photo / deskew / tight-crop / partial-crop / downscale / color), owner free-text narratives + attendee rosters, FORM header + Mileage-log entry.

## Known real bugs found in Phase A (to fix)
1. **Dup-image placement** — place() re-pastes the same multi-receipt PDF page 3× (one per line-item) + duplicated the coffee screenshot. Needs de-dup → one placed image per physical receipt.
2. **Grayscale leak** — OCR-preprocessed grayscale copies leak into the placed image; placed copy should source the original color image.
3. **KICC 세외수입 sector** — currently inferred PARKING/TOLLS; genuinely ambiguous (PARKING vs OTHERS). Needs deterministic rule or escalation.
4. **이종희 (까치화방[386])** — STAFF↔TRAVEL escalation (SAMSUNG EDS column); verify escalation fires correctly.
5. **Geometry** — uniform 3-per-row grid vs real layouts (full-width table rows1-16, full-width phone rows602-654); band targeting for non-uniform items.
6. **drawing2.xml hardcoded target** — safe for WK23 template but fragile across template variants.

## Round log
- **Phase A (done, wf wcy6pfidw):** target-spec + feasibility. Verdict: literal 98% infeasible (image-editing wall); Option A chosen.
- **Round 1 audit (done, wf w2jtr2z9i — 8 agents):** working copy `_hardening/skills/` created (original byte-untouched, baseline selftests match: orch27/verify56/classify22/merchant13/db21/place10/vote14/autodetect14). Comprehensive 6-dim audit found real CRITICALs — incl. C1 (classify silently routes known merchants to OTHERS when `--card` absent: store-db is biz_no-keyed, no name: alias, receipt_key falls back to name: → no signal → OTHERS-LOCAL escalate:false) and C2 (KICC 세외수입 no PARKING rule, no escalation). Full output = `/private/tmp/.../tasks/w2jtr2z9i.output`. Setup flagged **sandbox-closure prereq**: PROJECT_DIR resolves to repo root so cross-skill imports load ORIGINAL siblings → must repoint SKILLS to the copy before edits register in selftests.
- **Round 1 fix (assigned to CSO, plan-first gate):** CSO = ops executor. Procedure: close sandbox → read audit+tracker → propose prioritized fix plan (each: what / file:line / deterministic-reducible? / fix / regression risk) covering known criticals → **plan to surface:1 for master judgment + §5 BEFORE any execution** (no unauthorized apply) → worker executes on copy → re-run selftests → re-audit. ← current
- **Round 1 plan + §5 (done):** CSO submitted `_hardening/ROUND1_FIX_PLAN.md` (48 items / 6 batches A-F + sandbox-closure proven non-vacuously via mutation test, 177/177). §5 cross-model = **approve-with-changes** (Workflow 4-lens `wluv48s5m` + gemini-flash s4, converged). Plan coverage sound (no gaps) but **1 BLOCKER (C1 reverse-index misses 까치화방) + 6 MAJORS**. Master verdict + the 7 must-change + D1-D5 decisions = **`MASTER_GO_R1.md`** (D1 hybrid-WIRE-3-gates / D2 both+exact+escalate / D3 approve / D4 page-only / D5 reuse-pdftoppm). 
- **Round 1 execution (CSO-led, GO-with-changes):** CSO REVISEd plan per MASTER_GO_R1.md → batch-by-batch on copy w/ non-vacuous selftests → per-batch report → master judge+§5 → **MERGE GATE: original untouched until all-green + final §5 + 10-fresh-prompt gate + master/owner approval**.
- **Batch A (classify) + addendum — DONE:** 9 fixes (C1-C7,H8,H9) → §5 **pass-with-notes** (wf `wlnqagm6s` + gemini-flash converged). §5 adversarial edge-testing caught **3 latent majors** → master directed addendum: C5-NaN guard, C2 token-boundary (현장식당/주차장갈비/KICC카페 no longer mis-escalate; 세외수입 kept), C7 short-label normalize + 2 new non-vacuous selftests + doc/disclosure polish (headcount→people fixture, card-join causally inert 19=19, C6 0-added=structural). Worker-verified **38/38 selftest, no regression**, original `.claude/skills` byte-untouched (md5 1946695…, **promotion gap honored — live still PRE-FIX**, hardening lives only in `_hardening/skills/`).
- **★★RESUME POINT (fresh master start here):** **Batch A COMPLETE & §5-PASSED.** 9 fixes (C1-C7,H8,H9) + addendum (3 §5-found majors: C5-NaN, C2 token-boundary, C7 short-label) + finalization (C2 주차장 regression FIX bidirectional + C7 PARKING/TELEPHONE short + run-catch per-receipt resilience + str-guard) — ALL passed: independent adversarial Agent **PASS** (no new regression, mustFix NONE) + CSO bidirectional adversarial re-verify **PASS** + worker **41/0** non-vacuous. Live `.claude/skills` byte-untouched (promotion gap honored — hardening lives only in `_hardening/skills/`). **★★★CAMPAIGN COMPLETE + MERGE VERIFIED (live=hardened, restorable). NEXT = node-restart → 실 WK23 정산 (interactive)** — fresh-master §5 **RATIFIED** 2026-06-21 (Workflow `w31rkr8v9` RATIFY-GO, mustFix=0, 5-way convergence: Agent+CSO+worker41/0+gemini+master-WF; master-WF re-verified load-bearing facts directly — LIVE orig md5==`1946695…` byte-untouched, copy md5==`13b58150…`, selftest EXIT0 41/0, garbage→2 LOUD stderr re-escalate, 4 hardening symbols 0-count in orig / present in copy = non-vacuous). **Batch C GO issued to CSO = `_hardening/MASTER_GO_BATCH_C.md`** (verify-internal only; orchestrator-threading→Batch F; ANCHOR#2a fail-closed; V2-NOCARD no-false-FAIL; V3-INERT doc-honesty). Sequence A→C→B→D→E→**F[now·LAST]** (A,C,B,D,E done & §5-RATIFIED). **★Batch E §5: Workflow `wuqwv49zz` RATIFY-GO** (telephone routing 양방향[T-world NFC+NFD→telephone·plausible receipts 유지·precedence template-우선]·pdfinfo unconditional fail-closed[부재·rc!=0→RAISE·no silent 1page]·VOTE-1 occurrence[3 twin→3 keys no collapse]·MERCHANT-3 all-zero reject+NTS back-compat·3원본 md5 불변[d51ae8a7/0f5e5055/f662e580]·orchestrator 5dad9088 무접촉·autodetect23/vote17/merchant22; 2 LOW non-blocking→Batch-F note: 하이픈 'T-world' false-neg[실파일 space=정상·widen [\\s_-]?]·build_store_db non-selftest store-db 재기록[producer-output·input無]). **Batch F GO issued = `_hardening/MASTER_GO_BATCH_F.md`** (★LAST·integration-critical·orchestrator.py 첫 편집[md5 5dad9088→변경 EXPECTED]+classify C1 --card+master SKILL.md doc; V6-WIRING orch-half·DB-1 orch call·DH-3 sales_slips[KICC 매출전표 path reachable]·C1 --card fail-loud·doc-honesty DH-1/2/6; Batch-D LOW-2 dormant over-dedup 비활성 유지). ★CSO 새-pane 재기동 = master **재DEFERRED**(★[[cmux-tabmanager-fragility]] pane churn=TabManager 다운위험 + CSO 기능정상 + 잔여 1배치 + master 독립 §5가 실게이트 → system-risk>이득, soul §10); CSO 현 컨텍스트로 Batch F 감독·실제 degradation/60% 시에만 new-window식 신중 재기동. **★★Batch F §5 = HOLD-FIX**(master Workflow `w68vzblry`·3 lens 전부 FAIL·★worker 37/37+CSO PASS가 놓친 CRITICAL integration 버그를 master 독립 §5가 포착=게이트 작동 §6-b): **ROOT CAUSE**=orchestrator copy가 하드닝 _hardening 형제 아니라 라이브 un-hardened `.claude/skills` 부품 바인딩(L48-55 SKILLS=PROJECT_DIR/.claude/skills)→하드닝 死코드(live verify엔 run_dir/expected_week 無·live place엔 build_placements 無·live promote_week (week)만). 결함: ①CRITICAL fail-OPEN→비가역 place[verify(**_vk) TypeError→degrade 분기 L190-191이 run_dir/expected_week/sales_slips silently drop→wrong-week WK99인데 verdict PASS→place 도달=V6가드 死] ②CRITICAL KICC sales_slips drop→false V2-FAIL→정산 깨짐 ③HIGH dead db verdict-gate[L156 verdict= kwarg인데 live promote_week (week)만→TypeError·docstring 허위] ④CRITICAL selftest as-shipped 2FAIL+ImportError 크래시 exit1[worker 37/37은 cso_sandbox_baseline.py repoint서만=reproducibility gap] ⑤DOC SKILL.md L88 're-entrant' residue 1건(residue0 주장 허위). 완화(리스크↓·게이트 미통과): 7원본 byte-무손상·promotion gap·core fail-closed 순서 건전(promote store-db 비손상). **HOLD — 머지/fresh-prompt 게이트 절대 진행 안함.** FIX 지시서=`_hardening/MASTER_BATCHF_HOLDFIX.md`(6 mustFix·★fix=producer binding **relative-to-self**[copy tree서 _hardening 형제 바인딩=as-shipped 검증가능·post-merge .claude 바인딩 정확]+TypeError degrade를 **fail-CLOSED**[guard silently drop 금지·M7 교훈]+db gate+SKILL.md residue·★selftest AS-SHIPPED 37/0 직접실행[sandbox repoint 아님]) → worker FIX → CSO 재검증(as-shipped 재현) → master 재§5(`w68vzblry`-style). **✅ Batch F HOLD-FIX 재§5 = RATIFY-GO**(master Workflow `wc3xof3m9`·AS-SHIPPED 독립재현: selftest 직접 /tmp exit0 37/0·binding relative-to-self[L55 `SKILLS=Path(__file__).resolve().parents[2]`·place __file__=_hardening copy·old hardcode 잔재0]·★fail-OPEN 닫힘[wrong-week→ERROR→HALT place=0·real bound hardened verify로 재현]·TypeError degrade fail-CLOSED[ERROR stage verify-wiring·silent re-call 없음·single call site]·KICC settle[slip→consume·non-vacuous]·LOW-2 both placed·doc residue 0 exact-literal 7terms·orch copy 8e4b54d6·classify c5913e46 불변·7원본 byte-불변·정확히 2파일 변경 orch+SKILL.md). **★★전 6배치(A-F) §5-RATIFIED·스위트 end-to-end fail-closed 검증완료**(Batch F는 HOLD-FIX 1라운드=campaign iterate-until-clean 정상작동·master §5가 worker37/37+CSO 놓친 CRITICAL 포착·수정·재확인). LOW(비차단·merge-time 인지): _hardening/.claude place.py 2 copy 공존=binding 순서로 as-shipped 무해·머지시 .claude로 하드닝본 복사하며 수렴. **✅ 10-fresh-prompt 게이트 = PASS**(master Workflow `wkqsmbez3`·10/10·allFresh·allRan·zeroHallucinationAll·coverageAdequate·anti-gaming critic 독립검증: 10 신규토큰 전부 grep 0 hits=진짜 fresh·AS-SHIPPED _REUSED=True 실producer·__pycache__ clear·DB-1 3-arm refuse verbatim 재현·wrong-week→HALT 구조적[비가역 sink 보호]·KICC settle non-vacuous[no-slip→FAIL]·coverage 전 critical behavior). ★★스위트 완성·전 검증 통과. **NEXT = ★주인님 머지 go/no-go**(원본 .claude/skills 백업 후 하드닝본 교체=denylist④⑤·비가역·master/CSO/worker 무단 절대불가)=제시됨·대기. 머지 승인시→백업+하드닝본 cp→.claude(place.py 수렴)→실 WK23 정산(워커97%·CSO elevated=context-restart 필요). **★주인님 머지 승인 = GO(2026-06-21·"머지+실 정산")** = denylist 클리어. **MERGE 집행중**(CSO·지시서 `_hardening/MASTER_MERGE_DIRECTIVE.md`: STEP1 bulletproof 백업[tar `/Users/tajun/spJavis/expr_premerge_backup_20260621.tgz`·tree밖·VERIFY 후 진행]→STEP2 7스킬 cp _hardening→.claude→STEP3 AS-SHIPPED 머지 .claude orchestrator selftest 37/0+전파일 md5 .claude==_hardening+입력 PRISTINE→STEP4 origin 워치독 post-merge 재baseline→STEP5 보고). master 독립검증(AS-SHIPPED 머지 selftest+백업 복원성) 예정→노드 restart→실 WK23 정산. 복원=백업 tar `tar xzf`. **✅ MERGE-VERIFIED**(master 독립 Workflow `wvjzusojx`: AS-SHIPPED 라이브 .claude orchestrator selftest 직접 /tmp exit0 37/0·binding=.claude siblings[parents[2]·place __file__ under .claude·run_logical에 run_dir/expected_week 라이브]·8 script md5==하드닝·전 sub green[classify44/verify88/place30/db43/autodetect23/vote17/merchant22]·★백업 tar 추출→premerge 원본 md5 매칭=복원성 확인·입력 158 PRISTINE·fail-closed 라이브 재현[wrong-week→HALT place=0·KICC settle non-vacuous]). **★★★하드닝 스위트 LIVE 가동·캠페인 종결**(A-F 6배치+10-fresh-prompt 게이트+머지+머지검증 전부 통과; master §5가 CRITICAL binding 버그 1건 포착·수정·재확인=multi-layer 검증 가치 입증). **NEXT = 실 WK23 정산(주인님 승인된 (a))**: ①★노드 context-restart 필수(워커 s2 97%·CSO elevated→new-pane 재기동·WORKER_TODO/CSO_TODO 복원·TabManager 신중) ②orchestrate WK23(`raw-data/input/WK23_2026`·4 escalation[폴바셋·까치×2·스팟마트] 하드닝으로 재평가·KICC 매출전표 처리·실 place) — ★INTERACTIVE(vision OCR/handwriting HALT·escalation owner-control·name-selection=주인님 입력 필요). 복원백업=`/Users/tajun/spJavis/expr_premerge_backup_20260621.tgz`. **★Batch D §5: Workflow `wc5r13qhq` RATIFY-GO** (M6 byte-integrity deep: os.replace는 physical_verify PASS후에만·template MD5 불변·verbatim zip·[Content_Types] png 보존·rels↔blip·dedup 양방향[same-id×3→1·distinct→N·content-md5 media]·place orig `527b96aa`/copy `ddc6d1a4`·orchestrator `5dad9088` byte-identical·30/0; 2 LOW fail-closed residual: png-Default safe[template가 png 등록] + ★build_placements over-dedup DORMANT[런타임 미호출·selftest/docs만 → ★Batch-F 배선시 physical-id+image 공급해 id-keyed 유지·store|amount over-dedup 활성화 금지]). **Batch E GO issued = `_hardening/MASTER_GO_BATCH_E.md`** (extract/merchant: AUTODETECT-1 telephone_bill[owner T-world bill 처리]·AUTODETECT-2 pdfinfo page_count fail-closed[binary 부재→ERROR]·VOTE-1 occurrence-ordinal[dup date+amount 둘다 vote]·MERCHANT-1/2 consume-once 단일소유·MERCHANT-3 all-zero biz reject; orchestrator UNTOUCHED). E→F 경계서 CSO careful 새-pane 재기동 예정(또는 60% 조기). **★Batch B §5: Workflow `wq0r17ygb` RATIFY-GO** (DB-1 fail-closed[no-verdict promote→RuntimeError exit1·store-db 미작성]·DB-4 planning-collision reject[resolve-canonical·symlink/dot/parent 차단]·DB-2/3 crash-safety[detect-surfaces 非silent + PROMOTED-출처 det self-heal + snapshot-once]·real store-db `dd4e8453` 불변=namespace 격리·db orig `40e16d1f` 불변/copy `91e431b9`·orchestrator `5dad9088` byte-identical=Batch-F defer·selftest 43/0). **Batch D GO issued = `_hardening/MASTER_GO_BATCH_D.md`** (place-internal; PLACE-1/2 dedup[1 placement/physical·content-md5 media]·PLACE-3 grayscale-reject·PLACE-4 WK21 geometry[row_gap16]·PLACE-5 dynamic drawing; orchestrator UNTOUCHED·expected_pics single-source via place dedup·M6 placement-integrity 보존). ★CSO 새-pane 재기동 = master DEFERRED 판단(CSO 기능정상·cmux 재기동 operationally risky[de-master pitfall + watchdog gap]·master 독립 §5가 실게이트라 CSO context 非-bottleneck); CSO 현 컨텍스트로 Batch D 감독·60% 임계 시 careful 재기동. **★Batch C §5: Workflow `wslt6vsex` RATIFY-GO** (4-way: worker88/0+CSO bidirectional+master-WF; orchestrator `expensereceipt_orchestrator.py` orig==copy md5 `5dad9088…`=Batch-F defer honored · verify orig md5 `9305ab1f…` byte-unchanged · copy `ca3ab103…` · ANCHOR#2a fail-OPEN 0 [모든 degradation→ERROR→verdict≠PASS · vacuous SKIP 미계수 · multiread on-disk-count] · V2-NOCARD 양방향 sound [no silent no-card pass · no false-FAIL of cash/toll/telephone] · selftest 88/0 exit0). **Batch B GO issued = `_hardening/MASTER_GO_BATCH_B.md`** (db-internal; DB-1 orchestrator CALL→Batch F defer; DB-2/DB-3 crash-safety fail-closed; DB-4 path-collision reject). **Input RE-BASELINED 157→158** (manifest `7b70b33745f4` · owner WK22 매출전표 adaa9b9a 편입 · 구157=.157bak · macOS NFC/NFD path-consistency trap CSO-fixed) · watchdog `bcji0k7sy` scope=campaign 입력 전체(158 content-md5). CSO /clear ABANDONED 2026-06-21 — cmux 주입키(Return/Enter/space)가 Claude Code 슬래시명령 picker를 실행 못함=환경 한계(평문 메시지 제출은 정상). CSO는 현 컨텍스트로 Batch B 감독 진행; 60% 임계 시 새 pane 재기동식 clear(워커 패턴) 강구. 2 merge-time polish LOGGED (NON-blocking, both escalate=safe): fullwidth `ＫＩＣＣ` NFKC-fold + legacy `{confirmed:true}`-no-sector fall-through. Optional polish (NON-blocking, log for later/Batch-F): add `TOLLS`→PARKING/TOLLS + `PHONE`→TELEPHONE short aliases; fullwidth `ＫＩＣＣ` case-fold; the ~28 food-suffix whitelist over-escalates exotic restaurant names to a SAFE owner-confirm HALT (ANCHOR#2b-compliant, not a defect). MERGE GATE unchanged. Worker s2 on HOLD — resumes only on explicit CSO GO. (gemini-flash finalization §5 was still processing at this boundary — supplementary; the Agent+CSO convergence is the substantive PASS, and gemini had previously MISSED the 주차장 regression that the adversarial Agent caught.)
- **Focus (owner 2026-06-21):** **expensereceipt ONLY, quality-first.** stock-db + cys-vision hardening **DEFERRED until expensereceipt done**. (cys-vision 65-skill global install already DONE & verified; owner restarts CC — no further action.)
- **Context mgmt:** master did a /clear-refresh at this clean boundary (Batch-A+addendum done, worker HOLD) for supervision quality. Resume from the RESUME POINT above.
- **Nodes:** CSO s3 (ops executor, watchdog harness-tracked PID54613). gemini s4 (gemini-3-flash-preview — active §5 cross-model inspector). codex s5 (quota out til Jul-18). worker s2 (HOLD). §5 = gemini-flash + Workflow-adversarial.

## Verification gate (completion)
- Each round: fix findings on the copy → adversarial §5 re-verify → re-audit until DRY.
- Final: author **10 NEW** task-command test prompts (must differ from every prior round — anti-gaming), run them, require 100%: (1) zero hallucination, (2) matches SKILL.md original intent w/ cited evidence, (3) no further defects, (4) prompts fresh each round.
- PASS → present diff for merge. Else iterate (≤25 turns).
