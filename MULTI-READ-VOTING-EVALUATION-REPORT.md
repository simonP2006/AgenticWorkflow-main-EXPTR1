# N회 반복 판독 + 다수결 — 추가 검토 보고서

작성일: 2026-05-16
검토 대상: P1-2(이중 판독, ADR-044)의 잔여 한계 — "두 판독이 동일하게 오독하면 미검출"을 N회(예: 5회) 다수결로 보완하는 방안
원칙: 절대 기준 1(품질 — 토큰/시간 무시), 절대 기준 2(SOT), 절대 기준 3(CCP)

> **구현 상태 (2026-05-16)**: ✅ **구현·회귀 완료 (ADR-045, ADR-044 supersede)**. `scripts/aggregate_ocr_votes.py` 신규(적응형 3→7 + 신뢰 게이트 + 결합 불변식), `verify_ocr_consistency.py` 삭제, 프롬프트 4곳 동기화. 회귀 S1~S4 PASS(S4 계층방어 입증). 잔여 개선은 §3-3/§6 P2(영수증 원천 품질)뿐.

---

## 0. 결론 요약

1. **다수결은 옳은 방향이며, 2회 판독보다 엄밀히 우월하다 — 단 만능이 아니다.** OCR 오류는 두 종류이고 다수결의 효과가 정반대다:
   - **확률적(stochastic) 오류** — 같은 이미지를 읽을 때마다 결과가 흔들리는 경계 가독성 숫자: **다수결이 강력하게 교정**한다. (지배적 오류 유형)
   - **체계적(systematic) 오류** — 구겨짐/번짐으로 이미지가 *진짜로* 틀린 값(예: 6이 9처럼)을 더 지지하는 경우: **다수결은 오답을 다수가 선택 → "확신에 찬 오답"을 생성**한다. 이때 일치율을 신뢰 신호로 쓰면 오히려 위험하다.

2. 따라서 핵심 설계 원칙은 **"다수결 값을 채택"이 아니라 "일치율을 신뢰도 신호로 사용 + 기존 결정론 검증 계층(P1-1 톨 산술, 카드 대조, P0-2 파생 범위)과 반드시 결합"**이다. 다수결은 그 계층들을 *대체*하지 않고 *보강*한다.

3. **권고**: P1-2(2회)를 **N회 다수결 + 일치율 신뢰 게이트(적응형 표본)**로 확장(ADR-044 supersede). 비용은 절대 기준 1에 의해 무시하되, "독립성 확보 요건"(아래 §6)을 충족하지 않으면 N회가 1회로 붕괴하므로 그 요건이 핵심이다.

---

## 1. 핵심 질문 — 다수결이 "동일 오류"를 푸는가?

오류를 두 클래스로 나눠야 답할 수 있다.

| 클래스 | 정의 | 예 | 독립 N회 다수결 효과 |
|--------|------|-----|---------------------|
| **확률적** | 동일 이미지에 대해 판독값이 회마다 변동 (모델 샘플링 분산) | 흐릿한 `9,700`을 어떤 회는 9700, 어떤 회는 9100으로 | **강력 교정** — 다수가 정답으로 수렴 |
| **체계적** | 이미지가 *구조적으로* 특정 오답을 더 지지 (물리적 구김으로 6의 윗선 소실 → 누가 봐도 0에 가까움) | 구겨진 `960`이 모든 합리적 판독에서 `900` | **교정 불가, 악화** — 다수가 오답에 수렴, 일치율↑ = 거짓 확신 |

P1-2의 잔여 한계("두 판독이 동일하게 오독")는 **체계적 오류**를 가리킨다. 단순 다수결은 이 클래스를 풀지 못한다 — 오히려 N이 클수록 오답에 대한 일치율이 높아져 더 위험해질 수 있다.

---

## 2. 정량 모델

한 필드의 1회 독립 판독이 정답일 확률을 `p`, 오답은 독립이라 가정(확률적 클래스). N회 다수결이 정답일 확률:

`P_correct(N) = Σ_{k>N/2} C(N,k) · p^k · (1−p)^(N−k)`

| p (1회 정답률) | N=1 | N=3 | N=5 | N=7 |
|---|---|---|---|---|
| 0.70 | 0.70 | 0.78 | 0.84 | 0.87 |
| 0.80 | 0.80 | 0.90 | 0.94 | 0.97 |
| 0.90 | 0.90 | 0.97 | 0.99 | 0.997 |
| **0.45 (체계적)** | 0.45 | **0.43** | **0.41** | **0.39** |

해석:
- `p > 0.5`(확률적): N↑ → 정답률 단조 증가. **5회는 2회 대비 명백한 이득.**
- `p < 0.5`(체계적): N↑ → 정답률 단조 **감소**. 다수결이 오답을 *증폭*하고 높은 일치율로 위장한다. **이것이 단순 다수결의 함정.**

→ 다수결의 안전성은 "일치율이 높다"가 아니라 "**해당 필드가 확률적 클래스에 속하는가**"에 달려 있는데, 이는 사전에 알 수 없다. 그러므로 일치율은 *정답 보증*이 아니라 *escalation 신호*로만 써야 한다.

---

## 3. 설계 함의 — 다수결을 어떻게 안전하게 쓰는가

세 가지가 동시에 필요하다:

1. **일치율을 신뢰도 등급으로 변환** (정답 단정 금지):
   - **만장일치(N/N)** → 채택 + 로그. (단, 체계적 오류면 N/N도 오답일 수 있음 → 2·3과 결합 필수)
   - **강한 다수(예: ≥⌈0.8N⌉ 단일값)** → 채택 + Decision Log 기록
   - **약한 다수 / 동률 / 과반 없음** → **차단·해당 영수증 재판독·사용자 에스컬레이션** (silent 채택 절대 금지)
2. **기존 결정론 계층과 반드시 결합** — 다수결 산출값을 그대로 신뢰하지 않고:
   - P1-1 `verify_toll_integrity` (동일 구간 금액 일관성·amount=0·주 범위) — 체계적 톨 오독을 산술로 포착
   - `verify_card_matching` 카드 대조 — DINNER/STAFF/TRAVEL 금액을 카드 승인액과 대조 (체계적 금액 오독을 외부 사실로 포착)
   - P0-2 `derive_date_scaffold` 주 범위 — 체계적 날짜 오독을 범위로 포착
   다수결은 이 계층들을 **보강**할 뿐 대체하지 않는다. 즉 체계적 오류의 최종 방어선은 여전히 "독립 사실 대조"이지 다수결이 아니다.
3. **체계적 오류의 잔존 인정** — 카드 미대조 항목(톨·통신비)에서 *순수 체계적* 오독이 N/N 일치로 나오면 어떤 자동 계층도 못 잡는다. 이 잔여는 영수증 *원천 품질* 개선(고해상도 재촬영, 디지털 영수증 우선 — §6 P2)으로만 제거 가능하며, 자동 단계의 책임은 "낮은 일치율을 빠짐없이 표면화"까지다.

---

## 4. 구조적 데이터에서의 다수결 — 정렬 문제

단일 숫자가 아니라 *영수증 리스트*를 N회 읽으므로, 회마다 (a) 항목 수가 다르거나 (b) 순서가 다를 수 있다. 정렬 없이 다수결 불가.

설계:
- **항목 정체성 키**: 영수증 = `(section, date, time)`, 톨 = `(date, time, entry, exit)`. 회마다 이 키로 버킷팅.
- **필드별 mode**: 각 버킷에서 `amount`/`headcount` 등 placement-critical 필드의 최빈값 + 지지 횟수(support/N).
- **존재 자체의 투표**: 어떤 영수증 키가 N회 중 m회만 등장 → "이 영수증이 존재하는가"도 다수결 대상. m이 과반 미만이면 escalation(누락/환각 가능).
- **키 불안정 위험**: 시각을 회마다 다르게 읽으면(체계적) 같은 영수증이 다른 키로 분산. 완화: 키 매칭에 (date, amount±0) 1차 + time 근접 2차 폴백, 그래도 모호하면 그 영수증을 escalation으로 분류(자동 추정 금지).

---

## 5. 제안 설계 — N-read + 일치율 신뢰 게이트 (적응형)

`scripts/verify_ocr_consistency.py`(P1-2)를 일반화하여 `scripts/aggregate_ocr_votes.py` 도입(또는 옵션 확장):

1. **입력**: `research/ocr-results-1.json … ocr-results-N.json` (기본 N=5, 홀수 — 동률 방지).
2. **적응형 표본(비용 최적화 — 절대 기준 1과 양립: 품질 불변, 낭비만 제거)**:
   - 기본 3회 → 모든 placement-critical 필드 만장일치면 종료(3회로 충분).
   - 불일치 필드가 하나라도 있으면 **그 필드/영수증에 한해** +2회 추가 판독 → 최대 N=7.
   - 즉 쉬운 영수증은 3회, 흔들리는 영수증만 더 깊게. 품질 타협 없음.
3. **집계**: §4 정렬 → 필드별 (mode 값, support/N, 후보 분포).
4. **신뢰 게이트**:
   - 만장일치 → 채택, `planning/ocr-vote-audit.json`에 support 기록
   - 강한 다수(단일값 support ≥ ⌈0.8·N⌉) → 채택 + Decision Log
   - 그 외(약다수·동률·과반없음·존재 불일치) → **FAIL exit 1**, `research/ocr-vote-report.json`에 (영수증, 필드, 후보 분포) 핀포인트 → 해당 영수증만 재판독/에스컬레이션
5. **결합**: 채택된 합의본을 `ocr-results.json`으로 확정한 뒤에도 **P1-1·카드대조·P0-2 게이트는 그대로 전부 실행**(다수결은 선행 보강, 최종 방어선 아님 — §3-2).
6. **산출 신뢰 메타**: `ocr-vote-audit.json`에 필드별 support 비율 보존 → 사후 RLM 분석/주차 간 약점 영수증 패턴 학습 가능.

---

## 6. 독립성 확보 요건 (이것 없으면 N회가 무의미)

다수결의 전제는 **판독 간 독립성**이다. 동일 모델이 동일 프롬프트·온도 0으로 동일 이미지를 N번 읽으면 결과가 거의 동일(상관 1에 근접) → N회가 실효 1회로 붕괴, 확률적 오류조차 못 잡는다. 요건:

- **비-제로 샘플링 다양성**: 각 판독을 독립 세션/문맥에서 수행(1차 결과 미참조 — P1-2에서 이미 명문화). 가능하면 판독 순서·프롬프트 표현을 회마다 미세 변주.
- **이미지 전처리 다양화(선택, 강력)**: 회마다 다른 결정론적 전처리(대비 보정/이진화/디스큐 변형)로 입력을 *물리적으로* 다르게 → 체계적 오류의 일부를 확률적으로 전환(체계적→확률적 변환은 다수결이 풀 수 있는 영역으로 끌어옴). 단 이는 P2(이미지 원천 개선) 범주의 추가 작업.
- **온도/모델 메모**: 결정론적 디코딩이면 다수결 이득이 거의 0 — 판독에 샘플링 다양성이 실재해야 함을 Step 3 지시에 명시.

---

## 7. CCP 기반 구현 계획 (착수 시)

- **Step 1 의도**: P1-2 잔여(동일 오독)의 *확률적* 부분을 N회 다수결로 제거, *체계적* 부분은 낮은 일치율로 표면화. 비용 무시(절대 기준 1).
- **Step 2 파급**:
  - `verify_ocr_consistency.py`(ADR-044) → 일반화 또는 `aggregate_ocr_votes.py` 신규로 **supersede**. ADR-045가 ADR-044를 Superseded 처리.
  - 프롬프트 3↔4곳: `wk.md`/`WK_workflow.md` Step 3(2회→적응형 3~7회 + 독립성 요건), Step 0 삭제 목록(`ocr-results-*.json` glob), phase1_admin 게이트 문구.
  - 결합 불변식: P1-1/카드대조/P0-2 호출 순서·존재 그대로 — 다수결은 그 *앞단* 보강(절대 기준 2: 검증 계층 SOT 불변).
  - 회귀 자산: 12개 백업을 N부 복제(동일=만장일치 PASS) + 1부 변조(약다수→FAIL) + 체계적 모사(전부 동일 오답→다수결 PASS하지만 P1-1/카드대조가 잡는지 *결합 회귀*로 입증) ← 이 결합 회귀가 핵심.
- **Step 3 변경 설계**: §5 단계 순서대로. 적응형 표본으로 비용 낭비만 제거(품질 불변).

---

## 8. 권고 및 결정 요청

**권고**: 채택하되 "단순 다수결"이 아니라 **적응형 N-read + 일치율 신뢰 게이트 + 기존 결정론 계층과의 명시적 결합**으로 구현한다. 핵심 가치는 두 가지다 — (1) 확률적 오류를 5회로 강력히 줄인다(귀하 제안의 정당성), (2) 체계적 오류는 다수결이 *못 푼다*는 사실을 설계에 내장하여 "확신에 찬 오답"을 방지하고 낮은 일치율을 빠짐없이 escalation한다.

> 이는 ADR-044(2회)를 대체하는 변경이며 Step 3 실행 모델을 바꾼다. 착수 여부 / N 기본값 / 적응형 채택 여부에 대한 지시를 요청한다.
