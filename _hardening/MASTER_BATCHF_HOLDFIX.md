# MASTER HOLD-FIX — Batch F (orchestrator-contract) — expensereceipt Option-A hardening

> Issued by: master (surface:1). To: CSO (ops executor) → worker s2.
> Status: **Batch F maximal §5 = HOLD-FIX** (master Workflow `w68vzblry`, all 3 lenses FAIL). Independent master §5 caught a CRITICAL integration bug that the worker 37/37 + CSO PASS MISSED (their green ran in the sandbox-repointed binding; the as-shipped orchestrator binds the un-hardened live tree). This is the gate working (§6-b: happy-green ≠ trustworthy).
> NOT a merge step. Copy-only, reversible. Iterate per the campaign's verify→fix→re-verify loop (≤25 turns).

## 0. ★ROOT CAUSE (single — fixes most criticals)
The orchestrator copy `_hardening/skills/expensereceipt/scripts/expensereceipt_orchestrator.py` (L48-55: `_find_project_dir` → AgenticWorkflow-main-EXPTR1; `SKILLS = PROJECT_DIR/.claude/skills`) binds its 6 sub-producers from the **LIVE, UN-HARDENED** `.claude/skills` tree — NOT the hardened `_hardening/skills/*` siblings. Verified READ-ONLY: live verify lacks run_dir/expected_week/check_consensus_disk/load_vote_files (grep=0); live place lacks build_placements (=0); live promote_week is `(week)` only (no verdict gate). So the Batch-A..E hardening is DEAD CODE when the orchestrator runs as-shipped; the worker/CSO 37/37 only held because `/tmp/cso_sandbox_baseline.py` repoints SKILLS to the hardened copies (ROUND1_FIX_PLAN §0 sandbox-closure).

### ★FIX DIRECTION (master judgment)
**Make producer binding RELATIVE-TO-SELF**: the orchestrator must import its sibling sub-skills from ITS OWN skill-tree parent (the dir containing `expensereceipt-*`), NOT a hardcoded `PROJECT_DIR/.claude/skills`. Then:
- In the copy tree `_hardening/skills/expensereceipt/` → binds `_hardening/skills/*` (hardened) → integration is independently verifiable PRE-merge, selftest passes AS-SHIPPED (run directly, no sandbox repoint).
- Post-merge `.claude/skills/expensereceipt/` → binds `.claude/skills/*` (then-hardened) → correct.
This makes the artifact self-consistent in any tree it is deployed in. (If there is a strong reason to keep .claude binding, the alternative is to gate-verify only post-merge — but that is WEAKER and leaves dead wiring in the shipped copy; relative-to-self is preferred and required unless you surface a blocking reason to master.)

## 1. mustFix (all 6 — each with a non-vacuous AS-SHIPPED selftest)
1. **[ROOT] Producer binding** — bind the HARDENED siblings (relative-to-self per §0). After the fix, live `run_logical` must expose run_dir/expected_week/check_consensus_disk; live place build_placements; db verdict-gated promote_week — because the orchestrator now resolves the hardened copies.
2. **[CRITICAL] fail-OPEN to irreversible place** — orchestrator L184-191: `verify_logical(...,**_vk)` raised TypeError against live verify, and the except-TypeError degrade branch (L190-193) SILENTLY re-called dropping run_dir/expected_week/sales_slips → a WRONG-week (WK99) vote-audit → verdict PASS → **place REACHED instead of HALT** (fail-OPEN to the xlsx os.replace sink). FIX: (a) binding fix makes the kwargs accepted; AND (b) ★the TypeError/degrade path must FAIL-CLOSED — if verify cannot accept the wiring kwargs, that is an ERROR (verdict≠PASS, HALT), NEVER a silent drop of the wrong-week/disk-vote-audit/sales_slips guard (M7 lesson). selftest (as-shipped): wrong-week vote-audit on disk → HALT, place_called=0.
3. **[CRITICAL] KICC settlement** — same degrade dropped sales_slips → KICC 매출전표 never consumed → false V2-FAIL → HALT. After the binding + fail-closed-degrade fix: sales_slip present → CONSUMED, settles (not V2-FAIL). selftest (as-shipped): KICC sales_slip → consumed, end-to-end OK.
4. **[HIGH] dead db promote gate + false docstring** — L156 calls `_real_promote_week(week, verdict='PASS', verdict_week=week)`; with the binding fix this now hits the hardened db `promote_week(week, verdict, verdict_week)` (verdict-gated). Confirm it works; the L148-149 docstring "double-checks verdict==PASS at BOTH layers" must be TRUE after the fix (or corrected). selftest: FAIL/ERROR verdict → no promote (db-layer gate fires).
5. **[CRITICAL] SELFTEST green AS-SHIPPED** — the copy `--selftest` currently emits 2 FAIL then CRASHES (ImportError build_placements absent, L510, exit=1). After the binding fix, re-run to a CLEAN 37/0 exit 0 **run DIRECTLY (as-shipped), not via the sandbox repoint**. ★Verify the LOW-2 over-dedup-inactive case actually EXECUTES (it currently crashes before asserting → two distinct same-day same-amount receipts → both placed must run green).
6. **[DOC] SKILL.md residue** — `_hardening/skills/expensereceipt/SKILL.md` line 88 still contains the literal `re-entrant` (count=1, confirmed python-re + per-file grep). Either rephrase to drop the literal term, or correct the honesty claim to "5/6 terms removed; re-entrant retained as meta-doc". §6-b: the residue==0 claim must be literally true or restated.

## 2. PROTOCOL (unchanged)
- Copy-only. All originals byte-untouched (verified: 7 .claude originals match recorded prefixes; promotion gap intact). Input 158 PRISTINE (`7b70b33745f4`). No write to raw-data/input.
- Batch F touches orchestrator + classify + master SKILL.md only (the binding fix is in the orchestrator). If the binding fix requires touching another copy, surface it to master FIRST.
- ★The decisive selftests must be run AS-SHIPPED (orchestrator executed directly so it binds the copy-tree hardened siblings), NOT only via `/tmp/cso_sandbox_baseline.py` repoint. The repointed run masked this bug.

## 3. Checkpoint
Worker implements the binding fix + fail-closed degrade + db gate + selftest-green + doc fix on the copy → AS-SHIPPED `--selftest` clean 37/0 exit 0 (the 2 named FAILs turn PASS, no crash, LOW-2 case executes) → full sandbox baseline green → input PRISTINE assert + 7 originals byte-unchanged → CSO independent re-verify (★re-run the orchestrator selftest AS-SHIPPED, reproduce the wrong-week→HALT and KICC→settle, doc grep 0 multi-method) → report to surface:1 → master re-§5 (Workflow `w68vzblry`-style, re-confirm the 3 lenses + the 6 mustFix closed). Only then → 10-fresh-prompt gate → owner-approval merge.
