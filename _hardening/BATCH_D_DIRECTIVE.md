# BATCH D — WORKER DIRECTIVE (place geometry/dedup/integrity) — CSO (surface:3) → worker s2

> Master Batch B §5 RATIFY-GO → Batch D AUTHORIZED on the COPY. You are a WORKER (not master); report to CSO (surface:3). Read also: `_hardening/MASTER_GO_BATCH_D.md` + `ROUND1_FIX_PLAN.md §4 BATCH D` (full per-item detail). Constraints below are master-binding, WIN on conflict.

## 0. BOUNDARIES (inviolable)
- Edit **ONLY** `_hardening/skills/expensereceipt-place/scripts/` (`expensereceipt_place.py` + its SKILL.md). **★Do NOT touch `expensereceipt/scripts/expensereceipt_orchestrator.py`** (stays byte-identical md5 `5dad9088c0bc61c3b2ac421a171580ce`). The **expected_pics single-source is achieved purely in place.py** (build_placements returns the deduped list = `run['placements']`; orchestrator.py:164 already does `len(run['placements'])`) — do NOT edit orchestrator.
- **★FIRST capture place ORIGINAL md5**: `.claude/skills/expensereceipt-place/scripts/expensereceipt_place.py` = `527b96aa31d4623495908076878e8f40` (CSO-captured) — re-assert UNCHANGED after the batch.
- Original `.claude/skills` byte-untouched · live PRE-FIX (no promotion) · input `raw-data/input/` read-only, manifest `7b70b33745f4` (158) assert before+after · place must have NO write-path to raw-data/input · selftests use a STAGED/SYNTHETIC template xlsx in tempdir (NEVER the real input) · no git/merge/other-batch.
- Det-reduction: each fix = new Python fn in the place copy + SKILL.md(copy) references/calls it.

## 1. ★★ PRESERVE M6 PLACEMENT-INTEGRITY (non-negotiable — place does delicate xlsx byte-surgery)
The existing place pipeline was adversarially SAFE-verified. PLACE-1..5 must **NOT regress ANY** of: verbatim zip (unchanged entries byte-identical) · `[Content_Types].xml` png Default preserved byte-identical · drawing rels ↔ blip `r:embed` correct · NO media/cNvPr/rId collision · twoCellAnchor in-bounds · **atomic `os.replace` ONLY after `physical_verify` PASS** · **input xlsx MD5 immutable**. Re-assert all at the checkpoint (your selftest must prove the placed workbook reopens clean + the source/template MD5 is unchanged).

## 2. THE FIXES (`expensereceipt_place.py` + SKILL.md)
- **PLACE-1 (crit, det) [dup-paste]:** new `build_placements(receipts_for_sector, band)` → exactly **1 placement per stable physical-receipt id** (no re-paste of the same multi-receipt PDF page 3×). Selftest: a multi-line-item / multi-receipt-page receipt → 1 placement (not N).
- **PLACE-2 (crit, det) [dup-paste]:** content-md5 media dedup — embed each unique image **ONCE**, reuse its rId for repeats (no fresh imageN/rId per duplicate). Coupled to PLACE-1. Selftest: duplicate source image → 1 embedded media, reused rId, NO media/rId collision.
- **expected_pics SINGLE-SOURCE (must-change 5):** build_placements returns the DEDUPED list (= run['placements']); the physical-gate count auto-reflects it via orchestrator.py:164 `len(...)`. **Achieve in place.py ONLY — do NOT edit orchestrator.** Selftest: deduped placement count == embedded-media count == physical-gate expected (no false-FAIL).
- **PLACE-3 (high, det) [grayscale]:** placement `source_image` = the COLOR original, NEVER the grayscale OCR-preprocessed copy. `_to_png_bytes` guard: if `Image.open(p).mode in ('L','LA','1')` (and a color original is expected) → raise/escalate (fail-closed). Selftest: grayscale source path → reject/escalate (color original required).
- **PLACE-4 (high, det) [uniform-grid]:** per-sector geometry — 3-per-row grid for photo sectors (DINNER/STAFF/TRAVEL); **full-width per_row=1** for TELEPHONE-LOCAL / PARKING-TOLLS (from the WK21 filled-week reference, `row_gap=16` ground-truth). `layout_3_per_row` reads per_row from the band (it already reads len(band['cols'])). Selftest: full-width band sizing vs WK21 reference; a non-uniform (phone/toll) item placed correctly, not forced into the 3-grid.
- **PLACE-5 (high, det):** `resolve_receipt_drawing(xlsx)` dynamic via the rels-graph (factor out of `_receipt_pic_anchors`) → correct write target; keep the `drawing2.xml` constant only as a fallback for the WK23 template. Selftest: a variant template (Receipt → a different drawingN) resolves correctly (no hardcoded-drawing2 break).

## 3. REQUIRED non-vacuous hardcoded selftests (minimum — each FAIL pre-fix / PASS post-fix)
multi-line-item → 1 placement/physical receipt (not 3×) · duplicate source image → 1 media, reused rId, no collision · grayscale source → reject/escalate · deduped count == media == physical-gate expected · variant template drawing resolved dynamically · **M6 integrity preserved** (placed workbook reopens clean, input/template xlsx MD5 immutable, atomic os.replace only after physical_verify PASS).

## 4. VERIFY + REPORT
- Run `python3 /tmp/cso_sandbox_baseline.py` → place selftest green (10 base + new; note structural place tests stage a synthetic template via tempdir) + 0 FAIL + cross-importers (orchestrator/verify) still green + others unchanged. Input PRISTINE (158, `7b70b33745f4`). place-original md5 `527b96aa…` unchanged. orchestrator.py `5dad9088…` unchanged.
- Report to **CSO surface:3** (plain text, no backticks/$/modal): [1] place-original md5 before==after + orchestrator md5 unchanged · [2] functions touched · [3] each fix + selftest FAIL-pre/PASS-post DATA · [4] **★M6 integrity re-asserted** (placed workbook reopens clean, source/template MD5 immutable, atomic-after-PASS) · [5] PLACE-1/2 dedup: N-line-item→1 placement, dup-source→1 media · [6] expected_pics single-source (deduped==media==gate) · [7] final counts + no-regression + boundaries + promotion gap. Then HOLD (no Batch E until master §5 + CSO GO). I (CSO) re-verify (dedup both ways + grayscale-reject + M6 integrity + orchestrator untouched) before reporting up. If ambiguous → STOP + ask surface:3.
ACK on surface:3, then begin.
